The CRS matrix colums will correspond to itemsets.
Thus we need a smart way to map column indices back to orginal itemsets


Alays ensure we can map back from the new columns 

matrix with colum indices that column indices to orginal column indices when

map_k = {0: frozenset(0), 1: froze


(0,4),(